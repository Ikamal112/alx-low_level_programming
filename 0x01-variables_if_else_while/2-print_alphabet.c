#include <stdio.h>

/**
 * main - prints alphabet in lower case
 * Return: Always 0
 */
int main(void)
{
	char lcl;

	for (lcl = 'a'; lcl <= 'z'; lcl++)
	{
		putchar(lcl);
	}
	puchar('\n');
	return (0);
}
